{
  "name": "song-metadata",
  "version": "1.1.1",
  "description": "Walks through a folder searching for audio files. Once an audio file is found, metadata can be found with either a local .json file containing all the metadata in json format or will use Spotify's public API to attempt to fill in the missing metadata.",
  "main": "index.js",
  "dependencies": {
    "chalk": "^1.1.3",
    "clear": "0.0.1",
    "ffmetadata": "^1.4.0",
    "fs-extra": "^1.0.0",
    "jsonfile": "^2.4.0",
    "request": "^2.75.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "nodemon": "^1.11.0"
  },
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "babel src --out-dir dist",
    "dev": "nodemon --exec babel-node src/index.js"
  },
  "keywords": [
    "lute",
    "compile",
    "audio",
    "file",
    "download"
  ],
  "author": "Connor Hergott",
  "license": "MIT"
}
